#include <Eigen/Core>

Eigen::VectorXd delta(4);          // 4×1 error vector
Eigen::VectorXd a_prev;            // previous activation vector (size set elsewhere)
Eigen::MatrixXd dW = delta * a_prev.transpose(); // outer product: gradient w.r.t. weights