#include <algorithm>
#include <vector>

int main() {
    auto square = [](int x) noexcept { return x * x; };

    std::vector<int> v{1, 2, 3, 4};

    // apply square in-place
    std::transform(v.begin(), v.end(), v.begin(), square);
}