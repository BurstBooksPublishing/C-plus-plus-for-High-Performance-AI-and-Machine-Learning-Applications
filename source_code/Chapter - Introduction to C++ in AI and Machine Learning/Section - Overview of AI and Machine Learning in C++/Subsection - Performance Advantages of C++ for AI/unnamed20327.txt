#include <vector>

int main() {
    std::vector<double> weights = {0.1, 0.2, 0.3};

    // Double every weight in-place
    for (double& w : weights) {
        w *= 2.0;
    }

    return 0;
}