#include <omp.h>
#include <vector>
#include <functional>

void parallelWork(std::size_t n, std::function<void(std::size_t)> task)
{
    #pragma omp parallel for schedule(dynamic, 100)
    for (std::size_t i = 0; i < n; ++i) {
        task(i);          // user-provided workload
    }
}