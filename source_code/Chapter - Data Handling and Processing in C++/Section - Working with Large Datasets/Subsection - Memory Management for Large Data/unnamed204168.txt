#include <boost/iostreams/device/mapped_file.hpp>
#include <stdexcept>

boost::iostreams::mapped_file_source file;
try {
    file.open("large_dataset.bin");          // memory-map the file
} catch (const std::exception& ex) {
    throw std::runtime_error("Failed to open large_dataset.bin");
}
const char* data = file.data();              // direct read-only access